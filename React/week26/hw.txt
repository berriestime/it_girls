1.  props - это механизм передачи данных в компоненты.
    Props можно использовать в функциональных компонентах. 
2.  Статью в блоге, конечно, нужно выделить в отдельный компонент.
3.  Можно. 
4.  JSX это расширение синтаксиса JS, он ближе к JS. 
    На html похож только визуально.
5.  props нам нужны для работы с данными, они как параметры, которые мы передаем в функции.
6.  <React.Fragment> можно заменить на какой-то один тег, например <div>
7.  Классовые и функциональные компоненты одинаковы по функциональности.
8.  Приложение можно написать только на функциональных компонентах.
9.  Для экспорта используем в конце файла команду export default ComponentName;
10. Верхний - <App>
    Нижний - <Block>
11. Например map
```
    {cats.map((cat) => (
        <Cat name={cat.name} url={cat.url} text={cat.text}></Cat>
      ))}
```