1. 4 вида условных операторов
    - if
    - if else
    - else if
    - switch
2.  let date = new Date();
    let time = date.getHours();

    (time < 10) ? alert( "Доброе утро!" ) :
      alert( "Добрый день!" );
3. Да. Любая не пустая строка будет true
4.  - true
    - false
    - true
5.  1) Добавить обработчик события в атрибут. (onclick="clickMe()")
    2) Использовать свойство DOM объекту. 
    (В скрипте получить доступ к DOM объекту элемента,
    записать в нужное свойство свою функцию).
    3) addEventListener. (element.addEventListener(event, handler[, options]);
    Универсальным будет addEventListener, потому что в нем можно хранить несколько 
    обработчиков на одно событие.
6. Не корректна. В метод button.onclick нужно записать функцию, 
    а в данном случае запишется результат функции hello.
7. keydown/keyup
8. Объект события - это объект, со своим контекстом, который хранит 
    информацию о деталях события. 
    Например, такие как координаты курсора. (event.type, event.clientX)
9.  == - не строгое равенство
    === - строгое равенство
    != - не равно
    !== - строгое неравенство
    >= / <= больше/меньше или равно
    > / < - больше/меньше